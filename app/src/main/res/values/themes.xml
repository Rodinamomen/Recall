<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.NoteApp" parent="Base.Theme.NoteApp" />
    <style name="ThemeOverlay.search" parent="Base.Theme.NoteApp">
        <item name="android:editTextColor">@color/lightGray</item>
        <item name="android:textColorHint">@color/lightGray</item>
    </style>
</resources>